# Keep history on the eMMC overlay, rather than the volatile /var directory.
DatabaseDir "/etc/vnstat"
CreateDirs 1
# Write accumulated counters every 15 minutes. A clean stop also saves them.
SaveInterval 15
